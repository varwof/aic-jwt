{
  "name": "aic-jwt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AIC-JWT reference implementation + serverless browser demo",
  "scripts": {
    "build": "node demo/scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test demo/test/demo.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.25.12",
    "typescript": "5.6.3"
  }
}
